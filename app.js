const dashboardModules=[['Choisir votre modèle','Décider entre création, revente MRR ou parcours hybride'],['Trouver une demande existante','Collecter les mots du marché et prioriser un problème'],['Valider avant de construire','Fixer vos seuils et mener un test de 7 jours'],['Construire une transformation','Créer le chemin minimum du point A au point B']];
const completed=new Set(JSON.parse(localStorage.getItem('dpb-completed')||'[0]'));let current=+(localStorage.getItem('dpb-current')||0);const list=document.querySelector('#modules-list'),course=document.querySelector('#course'),nav=document.querySelector('#course-modules');
function progress(){const pct=Math.round(completed.size/COURSE.length*100);document.querySelector('#done-count').textContent=completed.size;document.querySelector('#progress-label').textContent=`${pct}% complété`;document.querySelector('#progress-bar').style.width=`${pct}%`;document.querySelector('#course-progress-bar').style.width=`${pct}%`;document.querySelector('#course-progress-label').textContent=`${completed.size} sur ${COURSE.length} modules terminés`}
function dashboard(){list.innerHTML=dashboardModules.map((m,i)=>`<article class="module-row ${completed.has(i)?'done':''}" data-id="${i}"><span class="status-dot">${completed.has(i)?'✓':String(i+1).padStart(2,'0')}</span><div><b>${m[0]}</b><small>${m[1]}</small></div><span class="tag">ACTION + CHECKPOINT</span><time>◷ ${COURSE[i].time}</time></article>`).join('');list.querySelectorAll('article').forEach(x=>x.onclick=()=>openCourse(+x.dataset.id));progress()}
function side(){nav.innerHTML=COURSE.map((m,i)=>`<button class="course-module ${i===current?'active':''} ${completed.has(i)?'done':''}" data-i="${i}"><span>${completed.has(i)?'✓':String(i+1).padStart(2,'0')}</span><div><small>${m.phase}</small><b>${m.title}</b></div></button>`).join('');nav.querySelectorAll('button').forEach(b=>b.onclick=()=>lesson(+b.dataset.i))}
function lesson(i){current=Math.max(0,Math.min(COURSE.length-1,i));localStorage.setItem('dpb-current',current);const m=COURSE[current];document.querySelector('#lesson-position').textContent=`MODULE ${current+1} SUR ${COURSE.length} · ${m.time}`;document.querySelector('#lesson-kicker').textContent=m.phase;document.querySelector('#lesson-title').textContent=m.title;document.querySelector('#lesson-result').textContent=m.result;document.querySelector('#lesson-body').innerHTML=m.body;document.querySelector('#action-copy').textContent=m.action;document.querySelector('#checkpoint-copy').textContent=m.checkpoint;document.querySelector('#lesson-answer').value=localStorage.getItem(`dpb-answer-${current}`)||'';document.querySelector('#prev-lesson').disabled=current===0;document.querySelector('.lesson-scroll').scrollTop=0;side()}
function openCourse(i=current){course.classList.add('show');document.body.style.overflow='hidden';lesson(i)}function closeCourse(){course.classList.remove('show');document.body.style.overflow='';dashboard()}
document.querySelector('#lesson-answer').oninput=e=>localStorage.setItem(`dpb-answer-${current}`,e.target.value);document.querySelector('#complete-lesson').onclick=()=>{completed.add(current);localStorage.setItem('dpb-completed',JSON.stringify([...completed]));current<COURSE.length-1?lesson(current+1):closeCourse();progress()};document.querySelector('#prev-lesson').onclick=()=>lesson(current-1);document.querySelector('.course-close').onclick=closeCourse;document.querySelector('.lesson-exit').onclick=closeCourse;document.querySelector('.mobile-course-menu').onclick=()=>document.querySelector('.course-nav').classList.toggle('mobile-show');document.querySelector('#continue-btn').onclick=()=>openCourse();document.querySelector('#course-access').onclick=()=>openCourse();document.querySelector('.lesson-open').onclick=()=>openCourse(1);document.querySelector('#all-modules').onclick=()=>openCourse(0);document.querySelector('#plan-btn').onclick=()=>document.querySelector('.roadmap-head').scrollIntoView({behavior:'smooth'});document.querySelectorAll('.path-card').forEach(c=>c.onclick=()=>c.dataset.path==='mrr'?openCourse(19):openCourse(0));const search=document.querySelector('#search-panel');document.querySelector('#search-open').onclick=()=>search.classList.add('show');document.querySelector('#search-close').onclick=()=>search.classList.remove('show');document.addEventListener('keydown',e=>{if(e.key==='Escape'){search.classList.remove('show');if(course.classList.contains('show'))closeCourse()}});dashboard();if(location.hash==='#formation')openCourse();
